import AnalyticsPage from './page-client';

export const metadata = {
  title: 'Analytics',
  description: 'ACWR, volume trends, and Kinetic Impact Score',
};

export default function Page() {
  return <AnalyticsPage />;
}
