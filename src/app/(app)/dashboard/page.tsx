export default function DashboardPage() {
  return (
    <main className="p-6">
      <h1 style={{ fontFamily: 'var(--font-display)', color: 'var(--highlight)' }}
          className="text-2xl font-bold tracking-wider uppercase mb-2">
        COMMAND CENTER
      </h1>
      <p style={{ color: 'var(--text-secondary)' }} className="text-sm">
        Dashboard loading — Wave 2 agents will build this out.
      </p>
    </main>
  );
}
