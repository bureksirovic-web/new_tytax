import DashboardPage from './page-client';

export const metadata = {
  title: 'Dashboard',
  description: 'Training overview and quick stats',
};

export default function Page() {
  return <DashboardPage />;
}
