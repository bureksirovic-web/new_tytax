import ActiveWorkoutPage from './page-client';

export const metadata = {
  title: 'Active Workout',
  description: 'Log sets, reps, and rest timers',
};

export default function Page() {
  return <ActiveWorkoutPage />;
}
