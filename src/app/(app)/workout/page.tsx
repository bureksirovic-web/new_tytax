import WorkoutPage from './page-client';

export const metadata = {
  title: 'Workout',
  description: 'Start or manage your active workout',
};

export default function Page() {
  return <WorkoutPage />;
}
